meta {
  name: Create checker
  type: http
  seq: 1
}

post {
  url: {{API_URL}}/api/admin/checker/
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{AUTH_TOKEN}}
}

body:json {
  {
    "name": "Example checker",
    "location": "this can be left blank"
  }
}
