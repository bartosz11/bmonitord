meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "{{USERNAME}}",
    "password": "{{PASSWORD}}"
  }
}
